services:
  apply-job:
    build: .
    entrypoint: ["sleep", "infinity"]
    environment:
      - DASHSCOPE_V2_API_KEY=${DASHSCOPE_V2_API_KEY}
      - APIFY_API_TOKEN=${APIFY_API_TOKEN}
      - LLM_MODEL=${LLM_MODEL:-qwen3.6-plus}
      - LLM_BASE_URL=${LLM_BASE_URL:-https://dashscope-us.aliyuncs.com/compatible-mode/v1}
      - CDP_URL=${CDP_URL:-http://host.docker.internal:9222}
      - DATA_DIR=${DATA_DIR:-/app/data}
    volumes:
      - ~/docker_data:/app/data

  studio:
    build: .
    entrypoint: langgraph
    command: dev --host 0.0.0.0 --port 8123
    ports:
      - "8123:8123"
    environment:
      - DASHSCOPE_V2_API_KEY=${DASHSCOPE_V2_API_KEY}
      - APIFY_API_TOKEN=${APIFY_API_TOKEN}
      - LLM_MODEL=${LLM_MODEL:-qwen3.6-plus}
      - LLM_BASE_URL=${LLM_BASE_URL:-https://dashscope-us.aliyuncs.com/compatible-mode/v1}
      - CDP_URL=${CDP_URL:-http://host.docker.internal:9222}
      - DATA_DIR=${DATA_DIR:-/app/data}
      - LANGSMITH_API_KEY=${LANGSMITH_API_KEY}
      - WATCHFILES_IGNORE_PATHS=/app/data
    volumes:
      - ~/docker_data:/app/data
      - ./src:/app/src:ro
      - ./langgraph.json:/app/langgraph.json:ro
      - ./pyproject.toml:/app/pyproject.toml:ro
      - ./uv.lock:/app/uv.lock:ro
